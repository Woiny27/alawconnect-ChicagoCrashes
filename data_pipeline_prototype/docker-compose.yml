services:
  app:
    build:
      context: ..
      dockerfile: data_pipeline_prototype/Dockerfile
    working_dir: /app
    volumes:
      - .:/app
      - ../risk:/app/risk
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
